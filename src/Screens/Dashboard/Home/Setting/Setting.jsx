import {View, Text} from 'react-native';
import React from 'react';

const Setting = () => {
  return (
    <View
      style={{
        display: 'flex',
        alignItems: 'center',
      }}>
      <Text>Setting</Text>
    </View>
  );
};

export default Setting;
